// Consentration: By using quotation and connect string with + like java

'Total Cost: $' + (5+3);

// Interpolation: under a pair of back-ticks connect the strings & (numbers + Math calculation) with ${}

`Total Cost: $${5+3}`;